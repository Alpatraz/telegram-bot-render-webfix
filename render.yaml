services:
  - type: web
    name: telegram-bot-render-webfix
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: BOT_TOKEN
        value: replace_with_your_token
